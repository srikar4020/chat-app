# Core Features
- Real-time chat using WebSockets
- One-to-one private messaging
- Persistent chat history stored in database
- User authentication (Login / Signup)
- Responsive UI (Mobile + Desktop)

## ⚙️ Installation & Setup (Local)

```bash
git clone https://github.com/srikar4020/chat-app.git
cd chat-assesment
npm install
cd client
npm install
npm run dev

# How It Works

1. User logs in using authentication system  
2. Frontend connects to backend via Socket.io  
3. Messages are sent and received instantly  
4. Messages are stored in MongoDB  
5. UI updates in real-time  


## 📸 Screenshots

![App Screenshot](./client/outputscreenshots/output-screen.png)